Searches text across the attached workspace paths or a user-provided `rootPath` and returns matching file paths, line numbers, and snippets.

- Use this when you need to locate code, symbols, configuration keys, or error strings.
- Prefer focused queries over broad keywords.
- If the user gives a folder path that is not already attached to the session, pass it as `rootPath`.
